import { type AlignT, type PositionT } from '../types';
declare type PropsT = {
    align: AlignT;
    position: PositionT;
    sameWidth?: boolean;
};
declare const getModifiers: ({ align, position, sameWidth }: PropsT) => {
    [x: string]: boolean;
    '&sameWidth': boolean;
};
export default getModifiers;
//# sourceMappingURL=getModifiers.d.ts.map