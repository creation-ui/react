import { Tab, Tabs } from 'nextra-theme-docs'
import { SCRIPTS_TABS }from '@root/constants'


## Instalation

This component is part of the `@creation-ui/core` package. To install it just run:

<Tabs items={SCRIPTS_TABS}>
  <Tab>

    ```bash copy
     yarn add @creation-ui/core
     ```

  </Tab>
  <Tab>
    ```bash copy
     npm i @creation-ui/core
     ```
     </Tab>
</Tabs>
