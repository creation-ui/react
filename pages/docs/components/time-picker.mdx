# Time Picker

import { properties, TimePickerExample } from '@components/examples/time-picker'
import PropsTable from '@components/props-table'

<TimePickerExample />

## Component API

<PropsTable
  properties={properties}
  name='Time Picker'
  description={
    <>
      Rest of the props are the same as for the native <code>input</code> component.
    </>
  }
/>
