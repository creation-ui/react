import { Container } from '@components/container'
import { properties, ModalExample } from '@examples/modal'
import { UnfoldVariants } from '@components/unfold-variants'
import { ELEMENT_POSITION, ELEMENT_SIZES, Modal } from '@creation-ui/react'
import PropsTable from '@components/props-table'
import { ListOrTypes } from 'utils/list-or-types'

export const list = ListOrTypes([...ELEMENT_POSITION])

# Modal

```tsx copy
import { Button, Modal } from '@creation-ui/react'

export const ModalExample = (props: ModalProps) => {
  const [open, setOpen] = useState(false)

  const onClose = () => setOpen(false)
  const onClick = () => setOpen(true)

  return (
    <>
      <Button onClick={onClick}>Open modal</Button>
      <div>
        <Modal open={open} onClose={onClose} {...props}>
          <div className='mt-2'>
            <p className='text-sm text-gray-500'>
              Your payment has been successfully submitted. We've sent you an
              email with all of the details of your order.
            </p>
          </div>
          <div className='mt-4'>
            <Button onClick={onClose}>Got it, thanks!</Button>
          </div>
        </Modal>
      </div>
    </>
  )
}
```

<Container>
  <ModalExample />
</Container>


## Component API

<PropsTable
  properties={properties}
  name='Modal'
  description='Modal component props.'
/>
<PropsTable
  properties={properties}
  name='Modal.Title'
  description='Modal.Title component props.'
/>
