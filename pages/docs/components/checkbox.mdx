# Checkbox

import { CheckboxPlayground } from '@components/examples/checkbox'
import PropsTable from '@components/props-table'
import { properties } from '@components/examples/checkbox'

<CheckboxPlayground label='Yes, I like cookies' />

## Component API

<PropsTable
  properties={properties}
  name='Checkbox'
  description={
    <>
      Rest of the props are the same as for the native <code>input</code>{' '}
      component.
    </>
  }
/>
