# Avatar

import { Avatar } from '@creation-ui/react'
import { ELEMENT_SIZES, ELEMENT_STATUS } from '@creation-ui/react'
import { Container } from '@components/container'
import { UnfoldVariants } from '@components/unfold-variants'
import PropsTable from '@components/props-table'
import {
  properties,
  badgeProperties,
  placementProperties,
} from '@components/examples/avatar'

```tsx copy
import { Avatar } from '@creation-ui/react'
;<Avatar src='...' alt='John Doe' />
```

export const defaultProps = {
  variant: 'circle',
  size: 'sm',
  src: 'https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&ixlib=rb-1.2.1&auto=format&fit=crop&w=250&q=80',
  alt: 'avatar',
}

# Avatar

<Avatar {...defaultProps} />

## Sizes

This component supports 3 different sizes `sm`, `md`, `lg`:

<UnfoldVariants
  component={Avatar}
  variants={ELEMENT_SIZES}
  prop='size'
  componentProps={defaultProps}
/>

## Notifications badges

### Static dot only

<UnfoldVariants
  component={Avatar}
  variants={ELEMENT_SIZES}
  prop='size'
  componentProps={{
    ...defaultProps,
    badge: { type: 'dot', placement: { horizontal: 'right', vertical: 'top' } },
  }}
/>

### With 5 notifications

<UnfoldVariants
  component={Avatar}
  variants={ELEMENT_SIZES}
  prop='size'
  componentProps={{
    ...defaultProps,
    badge: {
      type: 'count',
      pulse: true,
      placement: { horizontal: 'right', vertical: 'top' },
      count: 5,
    },
  }}
/>

### With 99 notifications

<UnfoldVariants
  component={Avatar}
  variants={ELEMENT_SIZES}
  prop='size'
  componentProps={{
    ...defaultProps,
    badge: {
      type: 'count',
      placement: { horizontal: 'right', vertical: 'top' },
      count: 99,
    },
  }}
/>

### Circle with notification pulsing

<UnfoldVariants
  component={Avatar}
  variants={ELEMENT_SIZES}
  prop='size'
  componentProps={{
    ...defaultProps,
    badge: {
      color: 'primary',
      type: 'dot',
      pulse: true,
      placement: { horizontal: 'right', vertical: 'top' },
    },
  }}
/>

## Component API

<PropsTable
  properties={properties}
  name='Avatar'
  description='Avatar component props.'
/>
<PropsTable
  properties={badgeProperties}
  name='Badge'
  description='Detailed props of "badge" Avatar props'
/>
<PropsTable
  properties={placementProperties}
  name='Placement'
  description='Detailed props of "badge.placement" Avatar props'
/>
