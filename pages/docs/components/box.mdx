# Box

import PropsTable from '@components/props-table'
import { Container } from '@components/container'
import { Box, Button } from '@creation-ui/react'


```tsx copy
import { Box, Button } from '@creation-ui/react'

const Example = () => (
  <Box>
    <h5 className='text-zinc-900 text-xl leading-tight font-medium mb-2'>
      New Post
    </h5>
    <p className='text-zinc-700 text-base mb-4'>
      This is the preview of your content
    </p>
    <Button>Edit</Button>
  </Box>
)
```

Box is simple container with padding used among other places as Popover content wrapper

<Container>
  <Box>
    <h5 className='text-xl leading-tight font-medium mb-2'>Box Title</h5>
    <p className='text-base mb-4'>This is the preview of your content</p>
    <Button>Edit</Button>
  </Box>
</Container>

## Component API

<PropsTable
  properties={[
    {
      name: 'children',
      type: 'ReactNode',
      description: 'The content of the Box.',
    },
    {
      name: 'className',
      type: 'string',
      description: 'The class name of the Box.',

    },

]}
name='Box'
description={'Box properties'}
/>
