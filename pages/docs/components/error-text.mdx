import { Container } from '@components/container'

# ErrorText

Used in inputs to display an error message. When content is falsy then it won't be displayed.

import InstallationGuide from '@components/shared/installation-guide.mdx'

<InstallationGuide />

## Usage

```tsx copy
import { ErrorText } from '@creation-ui/react'

<ErrorText
  error={`The message`}
/>
```

import { ErrorTextExample } from '@components/examples/error-text'

<ErrorTextExample />

## Component API

import { properties } from '@components/examples/error-text'
import PropsTable from '@components/props-table'

<PropsTable
  properties={properties}
  name='ErrorText'
  description={'ErrorText component props'}
/>
