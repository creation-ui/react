# DatePicker

DatePicker component just styling around [React Datepicker package](https://reactdatepicker.com/).
Please refer to the [docs](https://reactdatepicker.com/) on how to use it


import { Container } from '@components/container'
import { properties, DatePickerExample } from '@components/examples/date-picker'
import { UnfoldVariants } from '@components/unfold-variants'
import { ELEMENT_POSITION, ELEMENT_SIZES } from '@creation-ui/react'
import PropsTable from '@components/props-table'
import { ListOrTypes } from 'utils/list-or-types'
import { Callout } from 'nextra-theme-docs'


```tsx copy
import { DatePicker, DatePickerProps } from '@creation-ui/date-picker'

export const DatePickerExample = ({ ...props }: DatePickerProps) => {
  const [startDate, setStartDate] = useState<Date | null>(new Date())
  return (
    <div className='flex flex-col gap-5'>
      <pre className='text-xs'>{JSON.stringify(startDate)}</pre>
      <DatePicker
        {...props}
        selected={startDate}
        onChange={date => setStartDate(date)}
      />
    </div>
  )
}
```

<Container>
  <DatePickerExample />
</Container>

## Component API

<Callout>
  To lookup the rest of the component props see [React Datepicker
  docs](https://reactdatepicker.com/)
</Callout>

<PropsTable
  properties={properties}
  name='DatePicker'
  description='DatePicker component props'
/>
