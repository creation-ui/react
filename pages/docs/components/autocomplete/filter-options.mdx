import PropsTable from '@components/props-table'
import { filterOptionProps } from '@components/examples/autocomplete'

# filterOptions

Options passed to `createFilterOptions` function similarly as MUI Autocomplete component.

<PropsTable
  properties={filterOptionProps}
  name='AutocompleteFilterOptionsConfig<
  T = AutocompleteOptionDefault
>'
  description='Filter options type'
/>
