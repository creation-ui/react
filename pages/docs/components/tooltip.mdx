# Tooltip

import { properties, TooltipExample, TooltipPlayground } from '@components/examples/tooltip'
import PropsTable from '@components/props-table'

<TooltipPlayground />

```tsx copy
import { Button, Tooltip } from '@creation-ui/react'

export const Example = props => {
  return (
    <Tooltip content='Tooltip content' position='top'>
      <Button variant='contained'>Submit</Button>
    </Tooltip>
  )
}
```

## Component API

<PropsTable properties={properties} name='Tooltip' description='Tooltip component props.' />
